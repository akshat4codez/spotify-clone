import { AuthForm } from '@/features/auth/auth-form';

export default function SignupPage() {
  return <main className="min-h-screen flex items-center justify-center"><AuthForm mode="signup" /></main>;
}
